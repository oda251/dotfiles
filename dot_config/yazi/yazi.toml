[mgr]
ratio = [ 0, 1, 2 ]

[opener]
# エディタ（Neovim）: block = true でターミナルを占有
edit = [
	{ run = 'nvim "$@"', block = true, desc = "Neovim" }
]
# 画像ビューア: MacOSなら 'open'、Linuxなら 'xdg-open' や 'imv' など
image = [
	{ run = 'open "$@"', desc = "Open Image" }
]
# ブラウザ（HTMLなど）
web = [
	{ run = 'open "$@"', desc = "Open in Browser" }
]

[open]
rules = [
	# 1. HTML専用ルール（一番上に持ってくるのが安全）
	{ name = "*.html", use = [ "web", "edit" ] },
	{ mime = "text/html", use = [ "web", "edit" ] },

	# 2. 画像ファイル
	{ mime = "image/*", use = [ "image", "edit" ] },

	# 3. テキスト・コード（HTMLより下に配置）
	{ mime = "text/*", use = "edit" },
	{ mime = "application/javascript", use = "edit" },
	{ mime = "application/json", use = "edit" },

	# 4. それ以外（フォールバック）
	{ name = "*", use = "edit" }
]

[flavor]
dark = "catppuccin-mocha"

[[plugin.prepend_fetchers]]
id = "git"
url = "*"
run = "git"

[[plugin.prepend_fetchers]]
id = "git"
url = "*/"
run = "git"
