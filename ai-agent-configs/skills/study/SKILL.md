---
name: study
description: "普遍的な概念・技術・理論を学ぶ Q&A セッション。「<X>について教えて」「<X>を学びたい」「study」「勉強したい」等の依頼で起動する。Out of scope: 現在のプロジェクト・リポジトリ・特定ファイルや関数・進行中の作業に関する質問 — コード解説や docs スキルで対応する。"
inputs:
  topic: 学習テーマ（例: TCP輻輳制御、useEffect、CAP定理）
  question: ユーザの最初の質問
---

学習を Q&A セッションとして進め、同時に vault に学習ノートとして蓄積する。最後にユーザの合図で記述式クイズを実施し理解を確認する。

## 適用範囲

このスキルは **題材が普遍的に通用する学習** に限定する。

| トリガーする | しない |
|---|---|
| 「Reactのレンダリングの仕組みを教えて」 | 「このSvelteKitプロジェクトのrendering modeどう設定してる？」 |
| 「JWTってどう動くの？」 | 「この repo のauth middlewareの流れ追って」 |
| 「CAP定理って？」 | 「我々のDB構成のトレードオフを整理して」 |

判断基準: 「同じ説明が **別のプロジェクトの読者にもそのまま価値を持つか**」。Yes → study。No（特定コード・特定構成への依存が強い） → 通常の会話 or docs スキルで対応。

ハイブリッド（一般概念 + 自プロジェクトでの実例）の場合は、**一般部分のみ** をノート化し、プロジェクト固有の話は会話に留める。それでも残したいなら docs スキルへ。

## フェーズ構成

```
[1] 初回質問への回答 + ノート作成
        ↓
[2] 追加質問ループ（性質に応じて QA 追記 / 本文更新 / 両方）
        ↓ ユーザの「クイズ」「テスト」等の合図
[3] 記述式クイズによる理解確認
```

## 共通ルール

- vault 操作は `obsidian` コマンド経由のみ。ファイルシステム直書き禁止
- 出典は **インラインで** 付ける（`~/.references/policy/documentation.md` に従う）
  - 出典が無い情報は **（未検証）** と明記
  - WebSearch スニペットは出典にしない。WebFetch で本文確認した上で引用
- 確認できない事項は捏造せず「わかりません」と明言
- 本文は学習者目線で書く。専門用語は初出時に簡潔な定義を添える

## [1] 初回回答 + ノート作成

1. ユーザの質問にまず会話で答える（簡潔に。詳細はノートで）
2. ノートを作成する

### ノートパス

```
note/{date}-{topic-slug}.md            # 通常はこちら
{org}/note/{date}-{topic-slug}.md      # 明確に特定リポジトリの学習なら
term/{word-slug}.md                    # ユーザが単語に明示言及したとき（後述「単語ページ」）
```

- `date` = `YYYY-MM-DD`
- `topic-slug` = kebab-case。題材がわかる粒度（例: `tcp-congestion-control`, `react-useeffect-deps`）
- `{org}` = `git remote get-url origin` から抽出。原則 repo-less でよい

### ノート構造

```markdown
---
tags:
  - purpose/study
  - topic/<topic>
  - tech/<tech>   # 該当時
---

# {タイトル}

> 学習開始: {date}

## 概要

{1〜3段落で要点。インライン出典付き}

## 詳細

{セクション分け。コード例・図（mermaid）も活用}

## 参考

- [{タイトル}]({URL})
```

`## QA` のような QA 用ラッパーセクションは **置かない**。追加質問は `### Q: ...` エントリとして末尾に直接 append する（後述）。

### 作成コマンド例

```bash
cat > /tmp/study.md << 'EOF'
---
tags:
  - purpose/study
  - topic/networking
  - tech/tcp
---

# TCP 輻輳制御

...
EOF
obsidian create vault=obsidian-vault path="note/2026-05-11-tcp-congestion-control.md" content="$(cat /tmp/study.md)"
```

作成後、`obsidian open vault=obsidian-vault path="..."` で開けることをユーザに伝える。

## 単語ページ (term/)

ユーザが特定の単語に明示的に言及した場合、note と並列に `term/` 配下の単語ページとして作成する（Scrapbox 風に単語間を相互リンク）。

ノート構造・タグ規約・フェーズ構成（初回回答 / 追加質問ループ / クイズ）は通常の study ノートと同じ。固有事項はパスとリンクのみ。

### トリガー

- 単発の用語質問（「<word> って何？」）
- study セッション中の派生質問でユーザが単語に明示言及したとき

### パス

```
term/{word-slug}.md
```

- `word-slug` = kebab-case。日付プレフィックスなし
- 例: `term/mutex.md`, `term/cap-theorem.md`, `term/tcp-fast-open.md`

### 同名衝突（辞書方式）

同じ `word-slug` で異なる語義が出てきた場合、新ファイルを作らず **同一ファイル内に `## {分野ラベル}` で語義セクションを並べる**。分野ラベルは frontmatter の `topic/*` `tech/*` タグと整合させる。

```markdown
# python

## プログラミング言語
...（概要・詳細・参考をこの下にぶら下げる）

## ギリシャ神話
...
```

手順:

1. 作成前に `obsidian read path="term/{slug}.md"` で存在確認
2. 既存あり → `obsidian append` で新語義セクション（`## {分野ラベル}` 以下）を追加。frontmatter の tags は既存とマージして `obsidian replace` で更新
3. 既存なし → `obsidian create` で新規作成

### リンクの貼り方

- term ページ作成時、本文に出現する単語のうち `term/` に既存ページがあるものは自動で `[[term/word]]` リンク化
- 親 study ノートから派生して term ページを作った場合、親ノートの該当箇所（QA エントリ末尾 or 関連セクション）に `[[term/word]]` を 1 箇所追加して逆リンクを張る
- 全文書を遡及してリンク化はしない

## [2] 追加質問ループ

追加質問が来たら **性質を分類** してから反映する:

| 質問の性質 | 例 | 反映先 |
|---|---|---|
| (A) 概念に関する新規質問 | 「Slow Start と Congestion Avoidance の違いは？」 | 末尾に `### Q: ...` で追記 |
| (B) 本文の記述に対する質問/疑問 | 「『閾値』って具体的に何の値？」「ここの説明があいまい」 | 本文の該当箇所を更新（明確化・補強・訂正） |
| (C) 両方の性質を持つ | 概念質問だが、答えると本文の弱い部分が露呈する | (A) と (B) の **両方** を行う |
| (D) 本文に要改善事項が発覚 | 回答中に本文の誤り・抜け漏れに気付いた | 本文を更新（QA追記は質問次第） |

### 判断手順

1. 質問を読み「ノート本文の特定箇所への言及があるか」を確認
   - あり → (B) または (C)
   - なし → (A) だが、回答を組み立てる過程で本文の弱点が見つかれば (C)/(D) に昇格
2. 回答を会話で返す
3. 上記表に従い vault を更新

### QA 追記 (A/C)

`obsidian append` で末尾に `### Q: ...` エントリとして追記する。

```markdown

### Q: {ユーザの質問を要約}

{回答本文。インライン出典付き}
```

```bash
cat > /tmp/qa.md << 'EOF'

### Q: ...

...
EOF
obsidian append vault=obsidian-vault path="note/2026-05-11-...md" content="$(cat /tmp/qa.md)"
```

### 本文更新 (B/C/D)

`obsidian read` で現在の本文を確認 → 該当箇所を特定 → `obsidian replace` または書き直しで更新。

- 単純な差し替えは `obsidian replace` を使う
- 大きな書き換えは該当セクションを `obsidian read` → 編集 → 該当セクションを置換
- 変更の意図（何を明確化したか）は **コミットメッセージのように本文構造で表現** する。本文中に「追記:」「訂正:」などのメタ注釈は残さない（ノートは最終状態であるべき）

## [3] クイズフェーズ

### 起動条件

**ユーザの明示的合図のみ** で起動する。Claude から自発的に「クイズしますか？」と促してはいけない。

合図の例: 「クイズ」「クイズして」「テスト」「確認テスト」「理解度チェック」「quiz」「test」

### 進め方

1. 起動時にユーザに以下を確認:
   - 出題数（デフォルト: 3問）
   - 出題範囲（デフォルト: ノート全体）
   - 難易度（デフォルト: 中。「基礎中心」「応用含む」など）
2. **記述式（自由回答）** で1問ずつ出題。一度に複数問出さない
3. ユーザの回答に対し:
   - 模範解答と照合し、合致点・不足点・誤りを具体的に指摘
   - 出典をインラインで添えて補足
   - **誤答や答えられなかった項目はノートの該当箇所が弱い可能性が高い**。本文を読み直し、必要なら更新する（フェーズ2の (D) と同じ扱い）
4. 全問終了後、総評を出す:
   - 強い領域 / 弱い領域
   - 復習推奨セクション
   - ノートに改善を加えた場合はその要約

### 出題の質

- 単純な暗記より「なぜそうなるか」「いつ使うか」「他の概念との違い」を問う
- ノートに書いてある事実をそのまま聞くのではなく、ノートを理解していないと答えられない問いにする
- ただし範囲はノート本文の内容に限定する（範囲外を出して詰めるのは目的に反する）

## セルフチェック

完了前に確認:

- ノートのパス・タグ規約は docs スキル準拠か（`~/.references/policy/documentation.md` および `note/{date}-{slug}.md` / `term/{word-slug}.md`）
- 全ての事実にインライン出典 or 未検証マーカーが付いているか
- 追加質問の性質分類（A/B/C/D）に従って QA 追記 / 本文更新を正しく行ったか
- term ページ作成時、同名衝突を `obsidian read` で確認し辞書方式（`## {分野ラベル}`）で追加したか
- term ページ作成時、本文の既知 term への `[[term/word]]` リンク化と、親ノートからの逆リンクを張ったか
- クイズはユーザの明示合図を待ったか（自発的に促していないか）
- クイズで露呈した弱点をノートに反映したか
