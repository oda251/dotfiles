---
name: research-planning
description: 調査の起点。調査テーマに対してアクセスすべきリソースを列挙し、アクセス戦略を決定する。
---

## トリガー

調査・リサーチ・比較・情報収集が必要なとき。bootstrap から呼ばれる。

## 手順

### 1. 調査テーマの明確化

ユーザーの要求から以下を整理する:

- **調査テーマ**: 何を調べるのか
- **調査の目的**: なぜ調べるのか（実装判断、技術選定、現状把握など）
- **必要な粒度**: 概要レベルか、実装詳細レベルか

不明確な場合はユーザーに確認する。推測で進めない。

### 2. リソース列挙

調査テーマに対してアクセスすべきリソースをすべて列挙する。

| 調査対象 | 方法 |
|----------|------|
| ライブラリ・API仕様 | ctx7 CLI（最優先）→ WebSearch にフォールバック |
| 技術的事実 | 公式ドキュメント or WebSearch |
| 一般的事実 | WebSearch |
| ファイル内容 | Read |
| コードの挙動 | Read + トレース |

#### ctx7 CLI の使い方

ライブラリ・API のドキュメントを取得する CLI ツール。MCP サーバーではない。

```bash
# 1. ライブラリ ID を検索
ctx7 library "react" "hooks usage"

# 2. ドキュメントを取得
ctx7 docs /facebook/react "useEffect cleanup"
```

ctx7 が利用できない環境では WebSearch にフォールバックする。

### 3. アクセス戦略の決定

リソース数に応じて戦略を決める:

- **5件未満**: 自分で直接アクセスする
- **5件以上**: サブエージェントを並列起動する

リソース一覧と戦略が決まったら、ユーザーに提示して確認を取る。

## 次のスキル

→ **research-gathering** を呼べ。リソース一覧とアクセス戦略を引き継ぐ。
