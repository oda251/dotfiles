# バックエンドポリシー

## データモデルと DTO

- DTO を明確に定義する
- モデル層（DB スキーマ等）を SSOT とし、DTO はモデルから派生させる
- モデルと DTO を独立に定義しない。モデルの変更が DTO に自動的に反映される設計にする

## 環境変数・外部設定

- 環境変数は起動時にバリデーションし、型付きの設定オブジェクトに変換する。生の `process.env` / `os.environ` 等をアプリケーション内部で直接参照しない
- 必須・任意・デフォルト値をスキーマとして宣言し、不正な値は起動時に失敗させる
