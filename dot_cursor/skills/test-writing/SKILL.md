---
name: test-writing
description: 計画で定義した振る舞いをテストとして先に書く。
user-invocable: false
---

## エージェント構成

オーケストレーター（自分）が実装者を別エージェントとして立てる。
小規模タスク（単一ファイルの軽微な修正等）では自分で回してよい。

## スキップ条件

テストが不要なケース（設定ファイルの変更、既存テストが十分なリファクタリング）はスキップし、直接 implementation に進んでよい。

## 手順

**実装者**が計画で定義した振る舞いをテストとして先に書く。この時点ではテストは失敗する（Red）。

正常系・異常系・境界値を網羅すること。

### テスト依存の境界

テストが本番と乖離しないことが最優先。mock は乖離の原因になるため最小限にする。

| 依存の種類 | 方針 |
|-----------|------|
| ローカルで再現できる依存 | 本物を立ててテストする。mock しない |
| ローカルで再現できない依存 | プロトコルレベルで mock する。メソッドレベルの mock は避ける |
| 内部コード | mock しない |

テスト対象自体を mock しない。テスト失敗時は実装を直す（テストが間違っている場合を除く）。

## 次のスキル

→ **test-review** を呼べ。テストファイルのパスを引き継ぐ。
