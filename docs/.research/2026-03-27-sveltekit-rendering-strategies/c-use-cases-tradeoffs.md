# 各戦略の適切なユースケースと不向きなケース

## SSR

### 適しているケース
- ユーザーごとにパーソナライズされるページ（ダッシュボード、マイページ）
- リアルタイム性が求められるページ（EC の在庫、検索結果）
- SEO が重要かつコンテンツが頻繁に変わるページ
- 認証状態に依存するページ

### 不向きなケース
- コンテンツが静的で変更頻度が低いページ（CDN キャッシュで十分）
- サーバーコストを最小化したい場合
- TTFB を極限まで短縮したい場合（サーバー処理時間が加算される）

## SSG

### 適しているケース
- ドキュメントサイト、ブログ
- マーケティングページ、LP
- ヘルプセンター、FAQ
- 全ユーザーに同一コンテンツを表示するページ
- サーバーレスでホスティングしたい場合

### 不向きなケース
- ページ数が非常に多い場合（ビルド時間が膨大に）
- コンテンツが頻繁に更新される場合（更新のたびに再ビルド）
- ユーザー固有のデータを表示するページ
- SvelteKit 公式注記: SvelteKit は SSG 専用ツールではないため、非常に大量のページでは専用 SSG ツールほどスケールしない可能性

## ISR

### 適しているケース
- ニュースサイト、ブログ（定期的に更新されるが全ユーザーに同一内容）
- EC の商品カタログ（価格や在庫が数分〜数時間おきに更新）
- 大規模サイトでビルド時間を短縮したい場合
- SSG のパフォーマンスと SSR の鮮度の両方が欲しい場合

### 不向きなケース
- Vercel 以外にデプロイする場合（adapter-vercel 限定）
- ユーザー固有データのあるページ（キャッシュがリークするリスク）
- リアルタイム性が求められるページ（expiration の間は古いデータ）
- `prerender = true` と併用不可

## トレードオフ比較表

| 観点 | SSR | SSG | ISR |
|------|-----|-----|-----|
| TTFB | 中（サーバー処理） | 最速（CDN 配信） | 速い（キャッシュヒット時） |
| データ鮮度 | 常に最新 | ビルド時点 | expiration 間隔 |
| SEO | 良好 | 最良 | 良好 |
| サーバー負荷 | 高 | なし | 低（キャッシュミス時のみ） |
| ビルド時間 | なし | ページ数に比例 | 初回リクエスト時のみ |
| ホスティングコスト | 中〜高 | 最低 | 低〜中 |
| パーソナライズ | 可能 | 不可 | 不可 |
| インフラ制約 | なし | なし | Vercel 限定 |

## Sources

- https://svelte.dev/docs/kit/project-types
- https://svelte.dev/docs/kit/adapter-vercel
- https://www.pkgpulse.com/blog/ssr-vs-ssg-vs-isr-vs-ppr-rendering-2026
- https://dev.to/yugjadvani/csr-vs-ssr-vs-ssg-vs-isr-a-deep-dive-for-modern-web-development-33kl
