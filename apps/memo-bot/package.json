{
  "name": "memo-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/worker.ts --outdir dist --target browser --minify --format esm",
    "dev": "wrangler dev",
    "lint": "oxlint --type-check --type-aware",
    "format": "oxfmt",
    "test": "bun test"
  },
  "dependencies": {
    "neverthrow": "^8.2.0",
    "valibot": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/bun": "latest",
    "oxlint": "^0.15.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
