[project]
name = "claude-discord-bot"
version = "0.1.0"
description = "Discord bot for Claude Code notifications with bidirectional communication"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4",
    "aiohttp>=3.9",
]

[tool.uv]
dev-dependencies = []
