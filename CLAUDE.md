# chezmoi dotfiles

chezmoi で管理する dotfiles リポジトリ。

## ai-agent-configs/

AI コーディングツール（Claude Code, Codex, Cursor）の共通設定ソース。

エージェント設定の追加・変更はすべて `ai-agent-configs/` 配下で行うこと。配布先（`dot_claude/` 等）は直接編集しない。

