.gitignore
.chezmoiconfig.toml
.chezmoistate
.git
README.md
CLAUDE.md
LICENSE
lefthook.yml
mise.toml
Makefile
scripts/
docs/
pulumi/
.github/
ai-agent-configs/
package.json
package-lock.json
node_modules/

# Windows-specific configs (ignore on non-Windows systems)
{{ if ne .chezmoi.os "windows" }}
AppData/
Documents/PowerShell/
Documents/WindowsPowerShell/
{{ end }}

# macOS-specific configs (ignore on non-macOS systems)
{{ if ne .chezmoi.os "darwin" }}
.config/ghostty/
Library/
{{ end }}

# fcitx5 日本語入力は WSLg 専用 (非WSL では配布しない)
{{ if eq .chezmoi.os "linux" }}
{{ if not (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
.config/systemd/user/fcitx5.service
{{ end }}
{{ else }}
.config/systemd/user/fcitx5.service
{{ end }}

# Linux/macOS-specific configs (ignore on Windows)
{{ if eq .chezmoi.os "windows" }}
pulumi/
ai-agent-configs/
lefthook.yml
Makefile
package.json
package-lock.json
node_modules/
{{ end }}

# Ignore all PowerShell scripts on non-Windows
{{ if ne .chezmoi.os "windows" }}
**/*.ps1
{{ end }}

{{ if eq .chezmoi.os "windows" }}
# Ignore all shell scripts on Windows
**/*.sh.tmpl
**/*.sh
*.sh

# Homebrew configuration files
dot_Brewfile.tmpl

# macOS LaunchAgents
Library/
{{ end }}

